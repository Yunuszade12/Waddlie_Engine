For wasm:
>>>>cargo build --target wasm32-unknown-unknown --release  
and 
>>>>>wasm-bindgen --out-dir ./wasm/ --target web target/wasm32-unknown-unknown/release/waddlie_core.wasm

for native u cant
at least for now
cargo build is just gona give u a window that u can just fly around like a fly and do absolutly nothing :)